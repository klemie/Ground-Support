version: '3'

services:
  client:
    container_name: react-client
    build:
      context: ./client
      target: dev
    volumes:
      - ./client:/app
    ports:
      - 3000:3000
    depends_on:
      - server

  server:
    container_name: express-server
    build:
      context: ./services/server
      target: dev
    volumes:
      - ./services/server:/app
    ports:
      - 9090:9090
